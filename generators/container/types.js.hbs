/*
 * 
 * {{properCase name }}
 * 
 */


// import propType definitions
import { shape, number, bool, string, oneOfType } from 'prop-types';

export const collectionData = shape({
  data: data
});

export const data = shape({
});