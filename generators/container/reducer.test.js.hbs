import {{ camelCase name }}Reducer from '../reducer';
import { DEFAULT_ACTION } from '../constants';

describe('{{ camelCase name }}Reducer', () => {

  let initialState;
  beforeEach(() => {
    initialState = {
    };
  });

   it('should return the initial state', () => {
    const expected = initialState;
    expect({{ camelCase name }}Reducer(undefined, {})).toEqual(expected);
  });

  it('should handle the DEFAULT_ACTION action correctly', () => {
    const action = { type: DEFAULT_ACTION };
    expect({{ camelCase name }}Reducer(undefined, action)).toMatchSnapshot();
  });

});




